export const officeIdOf = (r: any) => r?.Office_Id ?? r?.OfficeId ?? r?.Id ?? r?.id;
export const officeNameOf = (r: any) =>
  r?.OfficeName ?? r?.CompanyName ?? r?.Name ?? r?.name ?? String(officeIdOf(r) ?? "—");

export const privIdOf = (r: any) => r?.GroupRight_Id ?? r?.Id ?? r?.id;
export const privNameOf = (r: any) =>
  r?.GroupRight_Name ?? r?.GroupRightName ?? r?.Name ?? r?.Title ?? String(privIdOf(r) ?? "—");
